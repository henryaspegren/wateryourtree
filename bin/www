#!/usr/bin/env node
var debug = require('debug')('water-your-tree');
var app = require('../app');

app.set('port', Number(process.env.PORT || 3000));

var server = app.listen(app.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});
